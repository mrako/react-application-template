datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

generator client {
  provider = "prisma-client-js"
}

model Vote {
  id         Int     @id @default(autoincrement())
  option     String
}
